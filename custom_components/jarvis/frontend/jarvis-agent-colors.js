/* JARVIS V4.0.0 — manual pipeline colors and long-search visual state. */
const Panel=customElements.get('jarvis-panel-v4');
const COLOR_KEY='jarvis_pipeline_colors_v4';
const norm=v=>String(v||'').trim().toLowerCase();
const readColors=()=>{try{return JSON.parse(localStorage.getItem(COLOR_KEY)||'{}')}catch(_){return{}}};
const saveColors=v=>{try{localStorage.setItem(COLOR_KEY,JSON.stringify(v))}catch(_){}};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const shades=hex=>{const h=String(hex||'#00eaff').replace('#','');const n=parseInt(h.length===3?h.split('').map(c=>c+c).join(''):h,16);if(!Number.isFinite(n))return{color:'#00eaff',soft:'#00eaff99',faint:'#00eaff44',text:'#dffaff'};const r=(n>>16)&255,g=(n>>8)&255,b=n&255;const text=(r*299+g*587+b*114)/1000>170?'#061018':'#f4fdff';return{color:'#'+h.padStart(6,'0'),soft:'#'+h.padStart(6,'0')+'99',faint:'#'+h.padStart(6,'0')+'44',text}};

if(Panel&&!Panel.prototype.__jarvisManualAgentColorsInstalled){
 const baseTheme=Panel.prototype._jarvisThemeForAgent;
 Panel.prototype._jarvisThemeForAgent=function(label){const colors=readColors(),key=norm(label),custom=colors[key];const base=baseTheme?baseTheme.call(this,label):{name:'JARVIS',color:'#00eaff',soft:'#00eaff99',faint:'#00eaff44',text:'#dffaff',voice:0,rate:.92,pitch:.88};return custom?{...base,...shades(custom),name:base.name||String(label||'JARVIS').toUpperCase()}:base};
 Panel.prototype._jarvisPipelineColorRows=function(){const select=this._core?.shadowRoot?.getElementById('pipelineSelect');if(!select)return '<div class="note">Les pipelines apparaîtront après chargement du Core.</div>';const colors=readColors(),options=[...select.options].filter(o=>o.value||o.textContent?.trim());if(!options.length)return '<div class="note">Aucun pipeline détecté.</div>';return options.map(o=>{const value=String(o.value||'').trim(),label=String(o.textContent||value||'Pipeline').trim(),key=norm(value||label),fallback=this._jarvisThemeForAgent(label)?.color||'#00eaff',color=colors[key]||fallback;return `<div class="jv-agent-color-row"><span>${esc(label)}</span><input type="color" value="${esc(color)}" data-pipeline-color="${esc(key)}" data-pipeline-label="${esc(label)}"><button type="button" data-pipeline-reset="${esc(key)}" data-pipeline-label="${esc(label)}">AUTO</button></div>`}).join('')};
 Panel.prototype._jarvisInstallAgentColorSettings=function(){const panel=this.shadowRoot?.getElementById('settingsPanel');if(!panel||panel.querySelector('[data-v4-section="agent-colors"]'))return;const actions=panel.querySelector('.actions'),section=document.createElement('div');section.className='section';section.dataset.v4Section='agent-colors';section.innerHTML='<div class="title">🎨 COULEURS AGENTS / PIPELINES</div><div class="note">Les couleurs d’état Écoute / Réflexion / Recherche / Parole gardent la priorité pendant une conversation.</div><div class="jv-agent-color-body"></div>';actions?.before(section);if(!this.shadowRoot.getElementById('jarvisPipelineColorStyle')){const s=document.createElement('style');s.id='jarvisPipelineColorStyle';s.textContent='.jv-agent-color-row{display:grid;grid-template-columns:1fr 46px 52px;gap:8px;align-items:center;padding:8px 0;border-bottom:1px solid #00eaff12}.jv-agent-color-row input[type=color]{width:42px;height:30px;padding:0;border:1px solid #00cfff55;border-radius:6px;background:transparent}.jv-agent-color-row button{min-height:30px;border:1px solid #00eaff33;border-radius:7px;background:#062033;color:#dffaff;font-size:8px}';this.shadowRoot.appendChild(s)}this._jarvisRenderAgentColorSettings()};
 Panel.prototype._jarvisRenderAgentColorSettings=function(){const body=this.shadowRoot?.querySelector('.jv-agent-color-body');if(!body)return;body.innerHTML=this._jarvisPipelineColorRows();body.querySelectorAll('[data-pipeline-color]').forEach(input=>input.oninput=()=>{const colors=readColors(),key=norm(input.dataset.pipelineColor),label=input.dataset.pipelineLabel||key;colors[key]=input.value;colors[norm(label)]=input.value;saveColors(colors);this._jarvisApplyAgentTheme?.()});body.querySelectorAll('[data-pipeline-reset]').forEach(btn=>btn.onclick=()=>{const colors=readColors(),key=norm(btn.dataset.pipelineReset),label=norm(btn.dataset.pipelineLabel);delete colors[key];delete colors[label];saveColors(colors);this._jarvisApplyAgentTheme?.();this._jarvisRenderAgentColorSettings()})};
 const baseBoot=Panel.prototype._bootCore;Panel.prototype._bootCore=async function(){await baseBoot.call(this);this._jarvisInstallAgentColorSettings();this._jarvisRenderAgentColorSettings()};
 const baseToggle=Panel.prototype._toggle;Panel.prototype._toggle=function(id){const r=baseToggle.call(this,id);if(id==='settingsPanel')queueMicrotask(()=>this._jarvisRenderAgentColorSettings());return r};
 Panel.prototype.__jarvisManualAgentColorsInstalled=true;
}

customElements.whenDefined('jarvis-core-v4').then(()=>{
 const Core=customElements.get('jarvis-core-v4');if(!Core||Core.prototype.__jarvisLongSearchInstalled)return;
 const baseSetState=Core.prototype.setState;
 Core.prototype.setState=function(label,color){const text=String(label||'');if(this.__jarvisLongSearchTimer){clearTimeout(this.__jarvisLongSearchTimer);this.__jarvisLongSearchTimer=null}const result=baseSetState.call(this,label,color),core=this.shadowRoot?.getElementById('core');if(core)core.classList.remove('jarvis-long-search');if(/RÉFLÉCHIT|REFLECHIT/i.test(text))this.__jarvisLongSearchTimer=setTimeout(()=>{if(this.processing&&core){core.classList.add('jarvis-long-search');this.log?.('🔎 Recherche approfondie…')}},2500);return result};
 const baseRender=Core.prototype.render;
 Core.prototype.render=function(){const r=baseRender.call(this);const root=this.shadowRoot;if(root&&!root.getElementById('jarvisLongSearchStyle')){const s=document.createElement('style');s.id='jarvisLongSearchStyle';s.textContent='.core.jarvis-long-search .r1{animation-duration:5s!important}.core.jarvis-long-search .r2{animation-duration:3.6s!important}.core.jarvis-long-search .r3{animation-duration:6.5s!important}.core.jarvis-long-search .r4{animation-duration:2.8s!important}.core.jarvis-long-search .glow{animation:jarvisDeepSearch 1.15s ease-in-out infinite!important}.core.jarvis-long-search .soul{opacity:1!important}.core.jarvis-long-search .soul i{background:#ffb000!important;box-shadow:0 0 7px #ffb000,0 0 16px #ffb00077!important}@keyframes jarvisDeepSearch{0%,100%{transform:scale(.94);filter:saturate(1.2) brightness(.95)}50%{transform:scale(1.16);filter:saturate(1.7) brightness(1.22)}}';root.appendChild(s)}return r};
 Core.prototype.__jarvisLongSearchInstalled=true;
});
