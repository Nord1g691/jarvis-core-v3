{
  "config": {
    "step": {
      "user": {
        "title": "JARVIS Core Assistant",
        "description": "JARVIS is configured automatically; no entity IDs are required."
      }
    },
    "abort": {
      "single_instance_allowed": "Only one JARVIS Core V3 instance is allowed."
    }
  }
}
